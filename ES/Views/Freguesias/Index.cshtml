@model IEnumerable<ES_TP_EF.Freguesia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.morada)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.morada)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cod_postal }) |
            @Html.ActionLink("Details", "Details", new { id=item.cod_postal }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cod_postal })
        </td>
    </tr>
}

</table>
